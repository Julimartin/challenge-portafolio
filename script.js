//La validación del formulário con javascript es considerada un desafío extra, no es obligatório realizar esta validación para realizar su entrega


// Función para alternar la visibilidad del menú en versión mobile
function toggleMenu() {
    const menu = document.getElementById('navMenu');
    menu.classList.toggle('show');
}

// Función para cerrar el menú al hacer clic en un enlace en versión mobile
function closeMenu() {
    const navMenu = document.getElementById("navMenu");
}
